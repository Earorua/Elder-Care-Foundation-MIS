# Elder Care MIS 用户手册

## 1. 系统简介

Elder Care MIS（养老关怀基金会管理信息系统）是一套基于 Web 的综合管理平台，涵盖捐赠管理、人员排班、礼品物流、活动策划和财务报表五大业务模块，并通过基于角色的访问控制（RBAC）确保不同岗位的员工只能访问与其职责相关的功能。

**技术环境**：Flask + Bootstrap 5 + SQLite，浏览器访问 `http://127.0.0.1:5000`。

---

## 2. 安装与启动

数据库文件已内置于项目目录中，无需额外配置，解压后即可运行：

```bash
cd elder_care_gui
pip install -r requirements.txt
python app.py
```

启动后在浏览器打开 http://127.0.0.1:5000 即可进入登录页面。系统首次启动时会自动创建以下预置账户（见第 3 节）。

---

## 3. 账户与角色

系统预置了 4 个账户，对应 4 种角色：

| 用户名 | 密码 | 角色 | 可访问模块 |
|--------|------|------|-----------|
| `admin` | `admin123` | admin（管理员） | 全部模块 + 用户管理 |
| `finance_user` | `finance123` | finance（财务） | 仪表盘、捐赠管理、财务管理 |
| `coordinator` | `coord123` | event_coordinator（活动协调） | 仪表盘、人员管理、礼品管理、活动管理 |
| `viewer` | `viewer123` | viewer（只读） | 仅仪表盘 |

### 3.1 角色权限说明

- **admin**：拥有所有功能的完整访问权限，包括用户管理（添加/编辑/删除用户、分配角色）。
- **finance**：可查看和操作捐赠记录、捐赠者、捐赠分类、捐赠反馈、税务收据，以及拨款、其他收入和全部财务报表。
- **event_coordinator**：可查看和操作人员信息、排班、薪酬，礼品（礼品/批次/分发/配送/供应商），以及活动和捐赠者参与。
- **viewer**：仅可查看仪表盘统计数据，无法进入任何管理页面。

### 3.2 权限控制机制

权限在两个层面生效：

1. **界面层**：侧边栏仅显示当前用户有权访问的模块链接，无权限的模块不会出现在导航中。
2. **路由层**：即使通过 URL 直接访问无权限的页面，系统也会自动拦截并重定向到仪表盘，同时显示"权限不足，无法访问该页面"的提示。

---

## 4. 登录与退出

### 4.1 登录

1. 打开浏览器访问 http://127.0.0.1:5000
2. 左侧面板展示系统品牌信息、浮动动画效果和系统规模统计（6 个模块 / 19 张表 / 4 种角色）
3. 在右侧面板输入用户名和密码
4. 可点击密码输入框右侧的眼睛图标切换密码的显示/隐藏
5. 可勾选「Remember me / 记住我」保持登录状态
6. 点击「Sign In / 登录」按钮
7. 登录成功后自动跳转到仪表盘

登录页面右上角和顶部栏均提供语言切换按钮（详见第 15 节）。如果账户被管理员禁用，登录时会提示"账户已被禁用"。退出登录后会显示"已成功退出登录"提示，未登录时访问系统会显示"请先登录"提示——这些提示信息均支持中英文自动切换。

### 4.2 退出

点击侧边栏底部的「Logout」链接，系统会清除登录状态并返回登录页面。

---

## 5. 仪表盘

**路径**：`/dashboard`　　**所有角色可访问**

仪表盘是登录后的默认页面，顶部显示个性化问候语（根据时间自动切换"早上好 / 下午好 / 晚上好"+ 用户名），右侧显示数据更新时间。问候语和时间格式会随语言设置自动切换中英文。

页面展示 4 个统计卡片（带趋势指标）：

- 捐赠总额（历史累计）
- 捐赠者数量（已注册捐赠者）
- 活动数量（活动总数）
- 人员数量（员工/志愿者）

页面下方包含 6 个图表（Chart.js）：月度捐赠趋势、捐赠类型分布、性别分布、年龄分布、捐赠者地图（美国各州）、礼品分发概览、活动募资进度。所有图表的图例、数据标签和提示文字均支持中英文切换。

---

## 6. 捐赠管理模块

**所需角色**：`admin` 或 `finance`

### 6.1 捐赠记录（/donations）

管理所有捐赠交易记录。

- **查看**：表格展示捐赠者姓名、捐赠类型、金额、日期、礼品等信息。
- **添加**：点击页面顶部「添加」按钮，在弹窗中填写捐赠者、类型、金额、日期、是否可抵税等字段。
  - **捐赠类型**：下拉选择 Cash / Check / Wire Transfer / Credit Card / In-Kind。
  - **礼品**：下拉选择从礼品表动态加载的活跃礼品（如 Donor Thank-You Kit、Campaign Postcard Set 等），也可选择"-- None --"表示无礼品。
- **编辑**：点击对应行的编辑图标，修改后保存。
- **删除**：点击删除图标，确认后删除记录。

### 6.2 捐赠者（/donors）

管理捐赠者基本信息（姓名、邮箱、年龄、性别、地区）。操作方式同上。

### 6.3 捐赠分类（/categories）

管理捐赠类别（支持父子分类层级）。

### 6.4 捐赠反馈（/feedback）

记录和跟踪捐赠者的反馈信息，包括反馈类型、内容和处理状态。

### 6.5 税务收据（/receipts）

管理税务收据的开具记录，关联到具体的捐赠记录。

---

## 7. 人员管理模块

**所需角色**：`admin` 或 `event_coordinator`

### 7.1 人员（/persons）

管理基金会所有人员信息，包括：姓名、邮箱、电话、人员类型（员工/志愿者）、角色名称、入职日期、状态、生日、性别。

### 7.2 排班（/schedules）

管理人员的工作排班：

- 关联人员和活动
- 设置班次日期、开始/结束时间
- 标记是否缺勤、加班时长
- 排班类型和状态

### 7.3 排班看板（/schedule-board）

以 Kanban 看板形式查看某一活动的排班情况：

1. 在页面顶部下拉框选择一个活动
2. 看板按状态分为四列：Scheduled / In Progress / Completed / Absent
3. 每张卡片显示人员姓名、角色、班次时间、排班类型，以及出勤状态标签：
   - **On Time**（绿色）：已完成且未缺勤
   - **On Duty**（蓝色）：进行中且未缺勤
   - **Absent**（红色）：标记为缺勤或状态为 absent
   - **Pending**（灰色）：尚未开始
4. 看板下方的「Daily Attendance」区域按日期分组展示出勤详情：
   - 每个日期为一个可折叠的手风琴卡片，标题显示日期、到勤人数和出勤率百分比
   - 展开后以表格列出：人员、角色、班次时间、加班、出勤状态、备注

### 7.4 薪酬（/payments）

记录人员的薪酬发放：

- 关联人员
- 支付日期、金额、支付类型（salary/bonus 等）
- 支付周期（开始/结束日期）

---

## 8. 礼品管理模块

**所需角色**：`admin` 或 `event_coordinator`

### 8.1 礼品（/gifts）

管理礼品目录：名称、类型、单价、描述、当前库存、最低库存、是否启用。

系统预置了 8 款与 Chao Feng & Maui 绘本系列相关的礼品：

| 礼品名称 | 类型 | 单价 |
|----------|------|------|
| Chao Feng & Maui: The Last Adventure | Storybook | $10.00 |
| Chao Feng & Maui: Digital Edition | Storybook | $0.00 |
| Chao Feng & Maui: Animated Short DVD | Media | $5.00 |
| Campaign Postcard Set | Promotional | $2.50 |
| Donor Thank-You Kit | Donor Kit | $15.00 |
| Campaign Poster | Promotional | $3.00 |
| Chao Feng & Maui Picture Book (Chinese Edition) | Storybook | $10.00 |
| Social Media Banner Pack | Promotional | $0.00 |

### 8.2 批次（/batches）

管理礼品采购批次，关联供应商和礼品，记录批次数量、类型和日期。

### 8.3 分发（/distribution）

记录礼品的分发情况，关联批次、捐赠和配送信息。

### 8.4 配送（/delivery）

管理配送记录：配送公司、日期、状态、配送费用。

### 8.5 供应商（/suppliers）

管理供应商信息：名称、地址、电话、公司、联系人。

---

## 9. 活动管理模块

**所需角色**：`admin` 或 `event_coordinator`

### 9.1 活动（/events）

管理基金会举办的活动：

- 活动名称、类型、描述、地点
- 开始/结束日期
- 状态（计划中/进行中/已完成）
- 目标金额与实际金额

### 9.2 捐赠者参与（/donors-events）

记录捐赠者参与活动的关联关系和备注。

---

## 10. 财务管理模块

**所需角色**：`admin` 或 `finance`

### 10.1 拨款（/grants）

管理外部拨款记录：拨款名称、资助机构、拨款编号、金额、收款日期。

### 10.2 其他收入（/other-income）

管理非捐赠、非拨款的其他收入来源。

### 10.3 财务报表（/reports）

报表总览页面展示三个带图标的汇总统计卡片（总收入、总支出、净收入），下方的收入构成分区展示捐赠、拨款、其他收入、薪酬支出四个分项金额（各带彩色图标）。页面底部提供三个符合 GAAS（公认审计准则）的正式报表入口卡片（带悬停动画效果）。

每份报表均包含：
- **正式报表头**：机构名称、报表名称、报告期间、编制日期、货币单位、会计基础
- **财务报表附注**：可折叠的 Bootstrap 手风琴，包含会计基础、收入确认、存货计价等政策说明
- **审计追踪栏**：编制人 / 复核人 / 批准人签名及日期栏
- **打印功能**：点击「Print Report」按钮调用 `window.print()`，打印时自动隐藏侧边栏、导航栏、图表，仅输出正式报表内容

#### 10.3.1 业务活动报表（Statement of Activities）（/reports/income-statement）

符合非营利组织 GAAS 规范的收支报表，分三节：

**第一节 — 收入（Revenue）**：
- A. 捐赠收入 — 按捐赠类型分组，含小计
- B. 拨款收入 — 按资助机构分组，含小计
- C. 其他收入 — 按收入类型分组，含小计
- 收入合计

**第二节 — 支出（Expenses）**：
- A. 人员薪酬 — 按支付类型分组，含小计
- B. 项目支出 — 礼品采购成本 + 配送物流费，含小计
- 支出合计

**第三节 — 净资产变动（Change in Net Assets）**：
- 总收入 − 总支出 = 净资产变动额（即净收入）

页面底部附月度收入 vs 支出趋势柱状图（打印时隐藏）。

#### 10.3.2 财务状况报表（Statement of Financial Position）（/reports/balance-sheet）

符合非营利组织 GAAS 规范的资产负债表，分三节：

**资产（Assets）**：
- 现金及等价物：累计总收入 − 累计总支出
- 礼品库存（按成本计价，FIFO 法）
- 流动资产合计 / 总资产

**负债（Liabilities）**：
- 当前无记录负债，显示 $0.00

**净资产（Net Assets）**：
- 无限制净资产 = 总资产 − 总负债
- 负债与净资产合计（与总资产平衡）

附表 A：礼品库存明细（品名、库存数量、单价、总价值）及资产构成环形图（打印时隐藏）。

#### 10.3.3 功能性支出报表（Statement of Functional Expenses）（/reports/expenditure）

符合 GAAS 规范的支出分类报表：

- **三列结构**：项目服务（Program Services）| 管理与一般（Management & General）| 合计
- 各支出类别逐行列示，含合计行
- 附表 B：人员薪酬明细（按人员和支付类型分组）
- 支出分类占比环形图 + 月度支出趋势柱状图（打印时隐藏）

---

## 11. 用户管理（仅管理员）

**路径**：`/users`　　**所需角色**：仅 `admin`

管理员可以在此页面管理系统所有用户：

### 11.1 查看用户列表

表格展示所有用户的 ID、用户名、邮箱、角色（彩色标签）和启用状态。

### 11.2 添加用户

1. 点击「添加用户」按钮
2. 填写用户名（不可重复）、密码、邮箱
3. 选择角色：admin / finance / event_coordinator / viewer
4. 点击「添加」

### 11.3 编辑用户

1. 点击对应用户的编辑图标
2. 可修改：邮箱、角色、启用/禁用状态
3. 如需重置密码，在「新密码」字段输入新密码；留空则不修改
4. 点击「保存」

### 11.4 删除用户

点击删除图标并确认。注意：管理员不能删除自己的账户。

---

## 12. 通用操作说明

### 12.1 数据表格

所有管理页面采用统一的表格布局，支持：
- 页面标题旁显示记录总数标签（如 "12 records"）
- 搜索框实时过滤表格内容（针对各页面提供专属的搜索提示文字）
- 顶部「添加」按钮打开新增弹窗
- 每行末尾的编辑（金色按钮）/ 删除（红色按钮）操作按钮，悬停时有阴影反馈
- 表格行悬停时显示渐变高亮和首列缩进动画
- 操作成功后页面顶部显示滑入式提示条（绿色/红色/黄色/蓝色）

### 12.2 弹窗表单

所有新增和编辑操作通过 Bootstrap Modal 弹窗完成。弹窗带有毛玻璃渐变标题栏和弹性缩放动画，填写后点击提交按钮即可。

### 12.3 删除确认

删除操作会弹出居中的确认对话框，显示垃圾桶图标和待删除记录名称，需点击「Delete」按钮确认方可执行。

### 12.4 提示消息

系统操作后会在页面顶部显示提示消息：
- 绿色（success）：操作成功
- 红色（danger）：操作失败或错误
- 黄色（warning）：权限不足
- 蓝色（info）：一般提示（如退出登录）

---

## 13. 常见问题

**Q：忘记密码怎么办？**
A：联系管理员（admin 角色用户），在用户管理页面重置密码。

**Q：为什么侧边栏看不到某些模块？**
A：您的账户角色没有该模块的访问权限。如需开通，请联系管理员调整角色。

**Q：直接输入 URL 访问被拒绝？**
A：系统在路由层也做了权限校验，无权限的请求会被重定向到仪表盘并提示"权限不足"。

**Q：如何更改自己的密码？**
A：当前版本需要联系管理员在用户管理页面修改。

---

## 14. 界面设计说明

系统采用"Botanical Warmth"（植物暖调）设计主题，主要视觉特征：

- **配色方案**：鼠尾草绿（#5a8a6a）、赤陶色（#c07a56）、金色（#c8a45c）、奶油色背景（#f6f3ee）
- **字体**：标题使用 Playfair Display（衬线体），正文使用 Outfit（无衬线体），通过 Google Fonts 加载
- **侧边栏**：深森林绿渐变背景，金色高亮当前活动页面，底部退出链接有独立的红色调悬停效果
- **顶部栏**：毛玻璃效果（模糊 + 半透明），显示面包屑导航、实时时钟（随语言切换格式）、语言切换按钮和用户头像/角色信息
- **动画效果**：页面内容淡入、统计卡片交错加载、弹窗弹性缩放、登录页浮动形状和飘叶粒子效果
- **图表样式**：圆角柱状图、暖色调配色、深色自定义工具提示（含货币格式化），图例支持中英文
- **响应式设计**：平板及以下尺寸侧边栏自动收起为覆盖层模式，手机端隐藏登录页左侧面板
- **无障碍支持**：跳转到主内容的快捷链接、键盘焦点可见样式、减弱动效媒体查询
- **打印优化**：财务报表支持打印，自动隐藏导航栏和图表，仅输出正式报表内容

---

## 15. 中英文双语切换

系统支持完整的中英文双语界面，所有界面文字均可一键切换。

### 15.1 切换方式

- **登录页**：右上角的语言切换按钮（显示「中文」或「EN」）
- **系统内页**：顶部栏右侧的语言切换按钮

点击按钮后页面立即刷新并切换为目标语言。语言偏好保存在浏览器本地存储中，下次访问时自动保持上次选择的语言。

### 15.2 翻译覆盖范围

以下内容会随语言切换自动变更：

| 内容类型 | 示例 |
|----------|------|
| 侧边栏导航 | Dashboard ↔ 仪表盘、Donations ↔ 捐款 |
| 页面标题与面包屑 | Donation Records ↔ 捐款记录 |
| 表格列标题 | Amount ↔ 金额、Date ↔ 日期 |
| 按钮与操作 | Add ↔ 添加、Edit ↔ 编辑、Delete ↔ 删除 |
| 表单标签与占位符 | Search donors... ↔ 搜索捐款人... |
| 图表图例与数据标签 | Cash ↔ 现金、Male ↔ 男 |
| 图表坐标轴与提示 | Donations ($) ↔ 捐款（$） |
| 统计卡片 | Total Donations ↔ 捐款总额 |
| 问候语 | Good morning ↔ 早上好 |
| 日期时间格式 | Wed, Apr 8  02:30 PM ↔ 周三 4月8日  下午02:30 |
| 提示消息 | Logged out successfully ↔ 已成功退出登录 |
| 空状态提示 | No records found ↔ 暂无记录 |
| 删除确认弹窗 | Confirm Delete ↔ 确认删除 |
| 财务报表全文 | Statement of Activities ↔ 活动报表 |
